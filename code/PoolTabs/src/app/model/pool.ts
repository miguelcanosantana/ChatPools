/**
 * The Pool
 * @property {string} name : The object id in FireBase and pool's name
 * @property {string} description : Pool's description
 * @property {string} image : Pool's url image
 * @property {number} usersNumber? : Number of users who joined the pool
 */


export class Pool {

    name: string;
    description: string;
    image: string;
    usersNumber: number;
}
